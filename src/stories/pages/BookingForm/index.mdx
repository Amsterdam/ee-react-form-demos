import { Canvas, Meta, Source, Story } from '@storybook/addon-docs/blocks';
import * as BookingFormStories from './index.stories';
import BookingForm from '@/pages/BookingForm/BookingForm?raw';
import FormCheckboxInput from '@/pages/BookingForm/components/FormCheckboxInput/FormCheckboxInput?raw';
import FormTextArea from '@/pages/BookingForm/components/FormTextArea/FormTextArea?raw';
import FormDateInput from '@/pages/BookingForm/components/FormDateInput/FormDateInput?raw';
import FormTimeInput from '@/pages/BookingForm/components/FormTimeInput/FormTimeInput?raw';
import translate from '@/pages/BookingForm/utils/translate?raw';
import styles from '@/pages/BookingForm/BookingForm.module.css?raw';

<Meta of={BookingFormStories} />

# Booking Form

**<a href="https://github.com/Amsterdam/ee-react-form-demos/tree/main/src/pages/BookingForm" target="_blank">GitHub Repo URL</a>**

#### Table of Contents

- [Requirements](#requirements)
- [Overview](#overview)
- [Purpose](#purpose)
- [Key Points](#key-points)
- [How to Implement](#how-to-implement)
- [Usage](#usage)

## Requirements

- [React](https://react.dev)
- [Amsterdam Design System](https://designsystem.amsterdam)

## Overview

This demo implements a simple booking form with the following fields:

- **Name** (required)
- **Email** (required, must be a valid email format)
- **Start Date & Time** (required)
- **End Date & Time** (required, must occur after Start Date/Time)
- **Remote Meeting?** (optional checkbox)
- **Comments** (optional textarea)

### Validation Rules

- Name, Email, Start Date/Time, End Date/Time are required.
- Email must be a valid format.
- End Date/Time must be later than Start Date/Time.
- Remote Meeting? and Comments are optional.

## Purpose

The goal of this demo is to illustrate how to handle cross-field validation in forms. While individual field rules (e.g. "email must be valid") are straightforward, checking relationships between multiple fields—such as ensuring the end of a booking is after the start—requires more advanced validation logic.

This scenario is deliberately chosen because HTML5 validation alone struggles here. Native browser validation can flag missing values or malformed emails but it cannot easily express rules like `End time must be after Start time.` Even if you manage to wire it up, the resulting UX is inconsistent across browsers and hard to manage.

## Key Points

- **Conditional validation**: The `End Date` and `End Time` fields are validated against the chosen `Start Date` and `Start Time`. If the end occurs before the start, a custom error message is displayed.
- **Improved UX**: Instead of relying on native validation popups (which vary by browser and cannot always be styled consistently), the demo shows how you can display validation errors inline, next to the relevant field(s), for a smoother experience.
- **Error state handling**: This example highlights how `react-hook-form` makes it easier to manage complex rules and maintain a consistent error display across the form. TODO - how does this demonstrate react-hook-form?

## How to Implement

To implement this `BookingForm` there are six core files:

1. The React components:
    - [BookingForm.tsx](#--bookingformtsx)
    - [FormCheckboxInput.tsx](#--bookingformtsx)
    - [FormDateInput.tsx](#--formcheckboxinputtsx)
    - [FormTextArea.tsx](#--formtextareatsx)
    - [FormTimeInput.tsx](#--formtimeinputtsx)
2. The CSS styles (1 file)
    - [BookingForm.module.css](#2-cssstyles)
3. Utils (1 file)
    - [translate.ts](#3-utils)

*The `Loader` component is excluded from the docs, it is a generic preloader.*

## Usage

The following files are required:

### 1. React Components

#### - BookingForm.tsx

<Source code={BookingForm} />

#### - FormCheckboxInput.tsx

<Source code={FormCheckboxInput} />

#### - FormTextArea.tsx

<Source code={FormTextArea} />

#### - FormDateInput.tsx

<Source code={FormDateInput} />

#### - FormTimeInput.tsx

<Source code={FormTimeInput} />

### 2. CSS Styles

#### - BookingForm.module.css

<Source code={styles} />

### 3. Utils

#### - translate.ts

<Source code={translate} />
